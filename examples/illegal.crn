crn = {
    conc[one, 1], conc[n, n0],
    step[{
        add[z, one, znext],
        mul[znext, znext, zpow],
        cmp[zpow, znext]
    }],
    step[{
        ifLT[{ ld[znext, z] }],
        ifGE[{ ld[z, out] }]
    }]
}